name: Tests


on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      foo:
        type: string
        default: "iOS"

env:
  runner: ${{ fromJSON('["ubuntu-latest", "macos-latest"]')[inputs.foo == 'iOS'] }}
jobs:
  call:
    name: test 1
    runs-on: ${{ env.runner }}
    steps:
      - run: |
          echo ${{ inputs.foo }}
          echo ${{ runner.os }}
